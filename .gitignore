# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Credentials. The real ones live in ~/.libber/, outside the repo, but never
# let a local override or a stray token cache become committable.
.env
*.env
credentials.env
.cache
.cache-*

# Downloaded audio, in case anyone points the output folder at the repo
*.opus
*.m3u8
.libber/

# OS cruft
Thumbs.db
desktop.ini
.DS_Store

# Coverage artifacts
.coverage
.coverage.*
htmlcov/
